# dependencies
node_modules/

# next.js build output
.next/
out/

# the database + its WAL companions (data, not code — regenerated by scraping)
listings.db
listings.db-wal
listings.db-shm

# scrape artifacts & exports
sweep_batches/
sweep_merged.json
sweep_accumulator.json
*.xlsx

# os / editor
.DS_Store
Thumbs.db
*.log
